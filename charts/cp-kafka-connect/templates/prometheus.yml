apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ template "cp-kafka-connect.fullname" . }}-servicemonitor
  labels:
    release: prom-op
    app: {{ template "cp-kafka-connect.fullname" . }}-servicemonitor
    chart: {{ template "cp-kafka-connect.chart" . }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app: {{ template "cp-kafka-connect.fullname" . }}
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  endpoints:
  - port: metrics
    interval: 10s
    honorLabels: true
